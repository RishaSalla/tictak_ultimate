/**
 * 💾 DATA MODULE
 * بنك الأسئلة الرياضية + نصوص التعليمات
 */

export const MathGenerator = {
    // توليد أرقام عشوائية بين min و max
    rand(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; },

    getQuestion(mode) {
        let q = {}, a = 0, b = 0, target = 0;

        switch (mode) {
            case 'clash': // جمع وطرح بسيط (A + B = ?)
                a = this.rand(1, 9);
                b = this.rand(1, 9);
                q = { q: `${a} + ${b} = ?`, a: a + b };
                break;

            case 'void': // المجهول (A + ? = C)
                a = this.rand(1, 5);
                target = this.rand(6, 15);
                b = target - a;
                q = { q: `${a} + ? = ${target}`, a: b };
                break;

            case 'balance': // الميزان (3 + 4 = 2 + ?)
                a = this.rand(1, 5); b = this.rand(1, 5);
                let c = this.rand(1, a + b - 1);
                let d = (a + b) - c;
                q = { q: `${a}+${b} = ${c}+?`, a: d };
                break;

            case 'duality': // الثنائيات (أدخل رقمين مجموعهم X)
                target = this.rand(5, 15);
                q = { q: `x + y = ${target}`, targetSum: target, isDuality: true };
                break;
                
            default: // كلاسيك (لا أسئلة)
                return null;
        }
        return q;
    }
};

export const HelpData = {
    rules: `
    <h3>📜 قوانين اللعبة</h3>
    <p>1. الهدف: الفوز بـ 3 مربعات كبيرة متتالية (أفقياً، عمودياً، أو قطرياً).</p>
    <p>2. نظام الحركة: حركتك في المربع الصغير تحدد أين سيلعب خصمك تالياً.</p>
    <p>3. الفوز بالمربع: إذا حققت 3 رموز متتالية في مربع صغير، يصبح المربع ملكك بالكامل.</p>
    <p>4. الطريق المسدود: إذا تم توجيهك لمربع ممتلئ، يمكنك اللعب في أي مكان (Free Move).</p>
    `,
    math: `
    <h3>🧮 نظام الرياضيات</h3>
    <p>في الأنماط غير الكلاسيكية، يجب حل لغز رياضي لوضع رمزك:</p>
    <ul>
        <li><b>المواجهة:</b> حساب ناتج مباشر.</li>
        <li><b>المجهول:</b> إيجاد الرقم المفقود.</li>
        <li><b>الميزان:</b> موازنة المعادلة.</li>
        <li><b>الثنائيات:</b> إدخال أي رقمين مجموعهما يساوي الهدف.</li>
    </ul>
    `,
    powers: `
    <h3>⚡ القوى الخاصة</h3>
    <p>لكل فريق استخدام واحد لكل قوة في المباراة:</p>
    <ul>
        <li><b>☢️ النووي (Nuke):</b> تدمير محتويات مربع صغير بالكامل (إعادته للصفر).</li>
        <li><b>❄️ التجميد (Freeze):</b> منع الخصم من اللعب في مربع معين لدور واحد.</li>
        <li><b>👾 الهاك (Hack):</b> استبدال رمز الخصم برمزك في خانة واحدة (مع تشويش النظام).</li>
    </ul>
    `
};
